# 📋 项目开发规范

**项目名**: {项目名}  
**创建日期**: {日期}  
**流程版本**: v3.3 (集成 UI/UX Pro Max + 简化参考资料)

---

## 🎯 开发流程

本项目遵循严格的 **12 阶段开发流程**，每个关键阶段完成后必须等待用户确认。

### 流程概览

```
[0. 参考资料] → 读取架构文档 + 截图 → ⭐ 可选
    ↓
[1. 需求分析] → PRD 文档 → ⭐ 用户确认
    ↓
[2. 原型设计] → 设计系统 + DRD + HTML 原型 → ⭐ 用户确认
    ↓                    ↑
    │            UI/UX Pro Max 生成
    │            (67 风格/96 配色/57 字体)
    ↓
[3. 任务规划] → tasks.md → ⭐ 用户确认
    ↓
[4. 正式开发] → 源代码 (检索设计系统)
    ↓
[5. 测试用例] → test-cases.md → ⭐ 用户确认
    ↓
[6. 验证循环] → 验证报告 (6 阶段)
    ↓
[7. 测试执行] → 测试报告 → ⭐ 用户验收
    ↓
[8. 知识沉淀] → Obsidian 笔记
    ↓
交付完成
```

### 确认阶段

以下阶段完成后，**必须等待用户确认**才能继续：

| 阶段 | 确认内容 | 确认方式 |
|------|---------|---------|
| **参考资料** (可选) | 参考资料是否完整、是否需要补充 | 回复"确认通过" |
| **PRD 完成后** | 需求是否完整、架构是否合理 | 回复"确认通过" |
| **原型完成后** | 设计是否符合预期、交互是否流畅、UI 风格/配色/字体是否满意 | 回复"确认通过" |
| **任务规划完成后** | 任务拆分是否合理、工时评估是否准确 | 回复"确认通过" |
| **测试用例完成后** | 测试覆盖是否全面、覆盖率要求是否明确 | 回复"确认通过" |
| **最终验收** | 产品是否满足需求、是否有未修复 Bug | 回复"确认通过" |

---

## 📁 文档路径

### 项目文档

```
/Users/seacao/Documents/cjh_vault/Projects/{项目名}/
├── source/                 # ⭐ 参考资料 (可选)
│   ├── README.md           # 参考资料索引
│   ├── architecture.md     # 技术架构文档 (或链接到外部文件)
│   └── screenshots/        # 系统截图
│       ├── home.png
│       ├── login.png
│       └── ...
├── 01-PRD/
│   └── PRD-v1.0-需求说明.md
├── 02-DRD/
│   └── DRD-v1.0-设计说明.md
├── 03-原型/
│   └── prototype/
│       ├── index.html
│       ├── css/
│       └── js/
├── 04-开发任务/
│   └── tasks.md
├── 05-测试用例/
│   └── test-cases-v1.0.md
├── 06-验证报告/
│   └── verification-report-v1.0.md
├── design-system/          # ⭐ UI/UX Pro Max 设计系统
│   ├── MASTER.md           # 全局设计系统
│   └── pages/              # 页面级覆盖（可选）
│       └── dashboard.md
├── 07-开发日志/
│   └── YYYY-MM-DD.md
└── README.md
```

### 代码目录

```
~/Projects/personal/{项目名}/
├── CLAUDE.md               # 项目配置
├── source/                 # ⭐ 参考资料 (与文档同步)
│   ├── README.md           # 参考资料索引
│   ├── architecture.md     # 技术架构文档
│   └── screenshots/        # 系统截图
├── design-system/          # 设计系统（与文档同步）
│   └── MASTER.md
├── src/
│   ├── frontend/
│   └── backend/
├── tests/
└── package.json
```

---

## 📚 参考资料 (简化版)

### 使用场景

你通常只需要两类参考资料：

| 类型 | 说明 | 示例 |
|------|------|------|
| **技术架构文档** | 整体技术架构、模块设计、数据流 | `AgentScope 项目分析报告.md` |
| **系统截图** | 界面截图、交互流程 | 登录页、首页、设置页截图 |

### 快速创建参考资料

#### 方式 1: 复制外部文档

```bash
# 创建 source 目录
mkdir -p source/screenshots

# 复制架构文档 (或创建软链接)
cp /Users/seacao/Downloads/AgentScope 项目分析报告.md source/architecture.md

# 复制截图
cp ~/Downloads/screenshots/*.png source/screenshots/

# 创建简单索引
cat > source/README.md << 'EOF'
# 参考资料

## 技术架构
- [architecture.md](./architecture.md) - 整体技术架构文档

## 系统截图
- `screenshots/` - 界面截图
  - home.png - 首页
  - login.png - 登录页
  - settings.png - 设置页
EOF
```

#### 方式 2: 引用外部文档 (不复制)

如果文档太大或需要保持同步，可以创建软链接：

```bash
# 创建 source 目录
mkdir -p source/screenshots

# 创建软链接指向外部文档
ln -s /Users/seacao/Downloads/AgentScope 项目分析报告.md source/architecture.md

# 复制截图
cp ~/Downloads/screenshots/*.png source/screenshots/

# 创建索引
cat > source/README.md << 'EOF'
# 参考资料

## 技术架构
- [architecture.md](./architecture.md) - 整体技术架构文档 (软链接到外部文件)

## 系统截图
- `screenshots/` - 界面截图
EOF
```

### 参考资料索引模板

`source/README.md` 简化模板：

```markdown
# 参考资料

## 📋 资料清单

| 资料 | 类型 | 用途 | 相关阶段 |
|------|------|------|----------|
| architecture.md | 架构文档 | 了解整体架构、模块设计 | 需求分析、任务规划、正式开发 |
| screenshots/ | 截图 | 参考界面设计、交互流程 | 原型设计、正式开发 |

## 🔗 使用说明

1. **需求分析**: 参考 architecture.md 了解技术架构
2. **原型设计**: 参考 screenshots/ 中的截图设计界面
3. **任务规划**: 参考 architecture.md 规划模块任务
4. **正式开发**: 参考 architecture.md 和 screenshots/ 实现功能
```

### 使用参考资料

在 Claude Code 中：

```
# 启动时说明
"帮我创建一个类似系统，参考 source/ 目录下的架构文档和截图"

# 或在具体阶段引用
"参考 source/architecture.md 中的架构设计，分析需求"
"参考 source/screenshots/home.png 的布局设计首页原型"
"参考 source/architecture.md 中的模块设计，规划开发任务"
"参考 source/screenshots/login.png 实现登录界面"
```

### 最佳实践

1. **截图命名清晰** - 使用 `页面名_功能.png` 格式，如 `login_form.png`
2. **架构文档放前面** - 在 README.md 中说明架构文档的核心章节
3. **保持同步** - 代码目录和文档目录的 `source/` 保持同步
4. **简化为主** - 只放必要的参考资料，避免冗余

---

## 🎨 UI/UX Pro Max 设计系统

### 设计系统生成 (原型阶段必须执行)

```bash
# 基础用法
python3 ~/.claude/skills/ui-ux-pro-max-skill/.claude/skills/ui-ux-pro-max/scripts/search.py \
  "{产品类型} {行业} {关键词}" --design-system -p "{项目名称}"

# 示例：SaaS 仪表板
python3 ~/.claude/skills/ui-ux-pro-max-skill/.claude/skills/ui-ux-pro-max/scripts/search.py \
  "SaaS dashboard analytics" --design-system -p "MyApp"

# 持久化设计系统
python3 ~/.claude/skills/ui-ux-pro-max-skill/.claude/skills/ui-ux-pro-max/scripts/search.py \
  "SaaS dashboard" --design-system --persist -p "MyApp"
```

### 设计系统内容

| 资源 | 数量 | 说明 |
|------|------|------|
| **UI 模式** | 24 种 | Hero-Centric、Conversion-Optimized 等 |
| **UI 风格** | 67 种 | Glassmorphism、Minimalism、Brutalism、Bento Grid 等 |
| **配色方案** | 96 种 | 行业专属配色（SaaS、医疗、金融、电商等） |
| **字体配对** | 57 种 | Google Fonts 精选组合 |
| **图表类型** | 25 种 | 数据可视化推荐 |
| **技术栈** | 9 种 | React、Vue、Next.js、Tailwind、shadcn/ui 等 |
| **UX 指南** | 99 条 | 可访问性、交互、性能最佳实践 |
| **行业规则** | 100 条 | 行业专属设计推理规则 |

### 设计系统检索 (前端开发时)

在 Claude Code 中：

```
我正在构建 [页面名称] 页面。
请读取 design-system/MASTER.md。
同时检查 design-system/pages/[页面名称].md 是否存在。
如果存在，优先使用页面级规则。
如果不存在，仅使用 Master 规则。
现在，生成代码...
```

### 预交付检查清单

前端开发完成后，必须检查：

#### 可访问性 (CRITICAL)
- [ ] 颜色对比度 ≥ 4.5:1（正常文本）
- [ ] 触摸目标 ≥ 44x44px
- [ ] 表单使用 label 标签
- [ ] 图标按钮有 aria-label
- [ ] 图片有 alt 文本
- [ ] 键盘导航可见焦点状态

#### 交互 (CRITICAL)
- [ ] 所有可点击元素有 `cursor-pointer`
- [ ] 悬停状态有平滑过渡 (150-300ms)
- [ ] 按钮加载时禁用
- [ ] 错误消息靠近问题位置

#### 性能 (HIGH)
- [ ] 图片使用 WebP + lazy loading
- [ ] 尊重 `prefers-reduced-motion`
- [ ] 异步内容预留空间（防跳动）

#### 响应式 (HIGH)
- [ ] viewport meta 正确设置
- [ ] 移动端正文 ≥ 16px
- [ ] 无水平滚动
- [ ] z-index 层级定义清晰

#### 样式 (MEDIUM)
- [ ] 无 emoji 图标（使用 SVG：Heroicons/Lucide）
- [ ] UI 风格一致
- [ ] 字体配对合理
- [ ] 行高 1.5-1.75（正文）

#### 响应式断点
- [ ] 375px (手机竖屏)
- [ ] 768px (平板)
- [ ] 1024px (桌面小屏)
- [ ] 1440px (桌面大屏)

---

## 🔍 验证循环

代码完成后必须执行 **6 阶段验证循环**：

### Phase 1: Build Verification
```bash
npm run build 2>&1 | tail -20
```
**门禁**: 失败 → STOP 并修复

### Phase 2: Type Check
```bash
npx tsc --noEmit 2>&1 | head -30
```
**门禁**: 严重错误 → 修复

### Phase 3: Lint Check
```bash
npm run lint 2>&1 | head -30
```
**门禁**: 严重问题 → 修复

### Phase 4: Test Suite
```bash
npm test -- --coverage 2>&1 | tail -50
```
**门禁**: 覆盖率 ≥ 80%

### Phase 5: Security Scan
```bash
grep -rn "sk-" --include="*.ts" .
grep -rn "console.log" --include="*.ts" src/
```
**门禁**: 严重安全问题 → 立即修复

### Phase 6: Diff Review
```bash
git diff --stat
git diff HEAD~1
```
**门禁**: 意外变更 → 审查

---

## 🎭 Agent 团队

本项目使用以下 Agent 团队协作：

| Agent | 职责 | 模型 |
|-------|------|------|
| **orchestrator** | 总指挥，协调流程 | Opus |
| **planner** | 需求分析，任务规划 (参考 architecture.md) | inherit |
| **architect** | 架构设计 (参考 architecture.md) | inherit |
| **prototype** | UI/UX 设计 + 原型 (使用 UI/UX Pro Max，参考 screenshots/) | Opus |
| **coder** | 代码实现 (遵循设计系统，参考 architecture.md + screenshots/) | Sonnet |
| **tester** | 测试编写，执行测试 | Sonnet |
| **reviewer** | 代码审查 | inherit |
| **refactorer** | 代码优化 | inherit |
| **doc-writer** | 文档编写 | inherit |
| **obsidian-keeper** | Obsidian 笔记 | inherit |
| **security-reviewer** | 安全审查 | Sonnet |
| **performance-reviewer** | 性能审查 | Sonnet |
| **verifier** | 验证循环执行 | Sonnet |

---

## 📝 开发规范

### 代码规范
- 遵循项目代码规范
- 函数不超过 50 行
- 文件不超过 500 行
- 单一职责原则
- DRY (不要重复自己)

### 测试规范
- 测试覆盖率 ≥ 80%
- 核心模块覆盖率 ≥ 90%
- P0 功能 100% 覆盖
- 测试必须独立、可重复

### 提交规范
```
<type>(<scope>): <subject>

<body>

<footer>
```

类型：feat, fix, docs, style, refactor, test, chore

### 文档规范
- 所有文档使用 Markdown
- 文档版本化管理 (v1.0, v1.1, ...)
- 重要决策记录到开发日志
- 设计系统变更同步到 `design-system/`
- 参考资料索引保持更新

---

## ⚠️ 约束

1. **不要跳过确认环节** - 每个确认阶段必须等待用户回复
2. **不要并行修改同一文件** - 避免文件冲突
3. **不要留下 console.log** - 使用正式日志系统
4. **不要硬编码敏感信息** - 使用环境变量
5. **不要提交大文件** - 使用 Git LFS
6. **不要跳过设计系统** - 前端开发必须先检索设计系统
7. **不要使用 emoji 图标** - 使用 SVG 图标 (Heroicons/Lucide)
8. **不要忽略参考资料** - 有 source/ 时必须先读取索引

---

## 🔗 相关文档

- [Vibe Coding v3.3 技能](~/.claude/skills/vibe-coding-flow.md)
- [验证循环文档](~/.claude/agents/11-verifier.md)
- [Agent Teams 配置](~/.claude/agents/)
- [UI/UX Pro Max 设置](~/.claude/UI-UX-PRO-MAX-SETUP.md)
- [快速开始](~/.claude/QUICKSTART.md)
- [速查表](~/.claude/CHEATSHEET.md)

---

**最后更新**: {日期}  
**维护**: 项目团队  
**🦞 小龙虾出品**
